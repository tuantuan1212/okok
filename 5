local lp = game.Players.LocalPlayer
local stats = lp:FindFirstChild("leaderstats")

if stats then
    for _, v in pairs(stats:GetChildren()) do
        print("Tên:", v.Name, " | Giá trị:", v.Value)
    end
else
    print("Không tìm thấy leaderstats!")
end
